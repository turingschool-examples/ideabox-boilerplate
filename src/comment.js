class Comment {
  constructor(id, commentBody) {
    this.id = id;
    this.commentBody = commentBody;
    //comment will be an object that takes up a space in the idea class. must have ID to match it to the idea
    //argument that we pass in would be Idea.id
  }

  saveToStorage() {

  }

  deleteFromStorage() {

  }
}
